{
  "name": "flue-agent",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "flue dev --target cloudflare",
    "build": "flue build --target cloudflare",
    "deploy": "flue build --target cloudflare && wrangler deploy --config dist/flue_agent/wrangler.json"
  },
  "devDependencies": {
    "@flue/cli": "^1.0.0-beta.9",
    "@types/bun": "latest",
    "wrangler": "^4.107.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.11.0",
    "@flue/runtime": "^1.0.0-beta.9",
    "agents": "^0.14.2",
    "hono": "^4.12.27",
    "valibot": "^1.4.2"
  }
}